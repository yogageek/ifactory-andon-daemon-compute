apiVersion: v1
kind: Service
metadata:
  name: ifps-andon-daemon-compute-svc
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    run: ifps-andon-daemon-compute
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
